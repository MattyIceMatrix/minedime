# Security

This site is static: one HTML file served by GitHub Pages. It has no server, stores no data,
holds no credentials, and makes no network requests except loading fonts from Google Fonts.
A Content-Security-Policy blocks everything else.

To report a problem, open an issue without sensitive details and a private channel will be arranged.
